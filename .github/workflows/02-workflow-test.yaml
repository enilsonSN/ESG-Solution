name: 02 - Workflow Test

on:
  push:
    branches: 'feature/**'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Exibir o evento
        run: echo "workflow disparado por ${{ github.event_name }}"